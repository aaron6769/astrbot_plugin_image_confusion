# 图片混淆/解混淆插件

适用于 AstrBot 的图片可逆混淆插件。插件会按 Gilbert Curve 像素路径对图片像素进行可逆置乱，可用于生成混淆图，也可以用相同次数反向还原。

> 这是图片置乱工具，不是安全加密。只要知道算法和次数，理论上可以还原图片。

## 功能

- 支持图片混淆与解混淆
- 支持自定义次数，例如 `/混淆2`、`/解混淆3`
- 支持直接发送图片或引用图片后执行命令
- 输出 PNG 图片
- 支持 WebUI 配置输出图片保留时间，默认 24 小时
- 插件启动和每次处理图片前会自动清理过期输出图

## 安装

将插件目录放入 AstrBot 的插件目录：

```text
data/plugins/astrbot_plugin_image_confusion
```

安装依赖：

```bash
pip install -r requirements.txt
```

然后在 AstrBot WebUI 中重载插件，或重启 AstrBot。

## 使用

直接带图：

```text
/混淆
/混淆2
/解混淆
/解混淆2
```

引用图片：

```text
引用一张图片后发送：/混淆3
引用一张图片后发送：/解混淆3
```

次数说明：

- `/混淆` 等于混淆 1 次
- `/混淆2` 等于混淆 2 次
- `/解混淆2` 会按相同次数反向还原
- 次数上限为 50

## 配置

WebUI 配置项：

```text
输出清理 -> 输出图片保留小时数
默认值：24
```

说明：

- 输出图片保存于插件数据目录下的 `outputs` 目录
- 超过保留时间的 PNG 会自动清理
- 填 `0` 表示不按时间自动清理

## 算法简述

插件会将图片转换为 RGBA 像素数据，按图片宽高生成 Gilbert Curve 路径，再按照黄金比例相关偏移量移动像素位置。

混淆与解混淆使用相反方向的偏移：

```text
混淆：   shift =  offset * 次数
解混淆： shift = -offset * 次数
```

只要图片尺寸没有变化，并使用相同次数，就可以还原。

## 兼容

- AstrBot v4
- aiocqhttp / NapCat 适配器

## 许可证

MIT
